query getStores {
    stores {
        id
        name
    }
}

query getStore($id: String) {
    store(id: $id) {
        id
        name
        products {
            description
            id
            name
            price
        }
    }
}
